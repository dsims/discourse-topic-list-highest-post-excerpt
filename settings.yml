show_avatars:
  default: false
  description:
    en: Display excerpt avatar.
show_reply_icon:
  default: false
  description:
    en: Display a reply icon before latest post excerpt.
single_line:
  default: false
  description:
    en: Apply styling to limit excerpt to a single line.
categories:
  type: list
  default: ""
  list_type: category
  description:
    en: Apply plugin to the following categories. Blank applies to all of them.